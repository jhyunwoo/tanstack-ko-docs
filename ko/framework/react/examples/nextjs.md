# 예

이 간단한 예에서는 React-Query를 Next.js 데이터 가져오기 방법과 원활하게 통합하여 서버에서 queries를 가져오고 브라우저에서 이를 수화합니다.

데이터를 가져오고 변경하는 것 외에도 React-Query는 queries와 그 결과를 분석하여 데이터의 클라이언트 측 캐시를 구성합니다. 이 캐시는 추가 queries 및 mutations가 실행될 때 최신 상태로 유지됩니다.

이 예제를 실행하려면 다음을 수행하십시오.

- `npm install`
- `npm run dev`